# scripts

This folder exists so the top-level repo layout reads clearly.

No duplicate wrappers were added here during cleanup because the active commands already live under:

- `rl/scripts/`
- `rl/stage1_compare/`
- `rl/stage2_analysis/`

That choice avoids creating two competing entrypoint locations.
